# React Admin Panel

## Run locally

```bash
cd admin-panel
npm install
npm run dev
```

This panel connects to:

```txt
http://localhost:8000/api
```

Use the seeded admin account from the Django backend:

```txt
admin@fass.local / Admin123!
```
